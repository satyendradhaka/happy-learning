<!DOCTYPE html>
<html>
<head> 
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>Video streaming with Node.js</title> 
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
	<!-- Fonts -->
	<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
	<!-- Custom CSS -->
	<link rel="stylesheet" href="/css/forgot.css" type="text/css">
</head>
<body>
	<div class="container d-flex m-auto align-items-center h-100">
		<div class="col col-12 col-md-9 col-lg-6 mx-auto p-4 p-sm-5">
				<p>Password Reset</p>
				<form action="/login/forgot/reset/<%=resetToken%>" method="post">
					<div class="form-group">
						<label for="newPass">Enter new password</label>
						<input id="newPass" type="password" class="form-control" name="newPassword">
						<small class="form-text text-muted">Must be minimum 8 characters</small>
					</div>
					<div class="form-group">
						<label for="conPass">Re-enter the new password</label>
						<%if(error && error.length>0){%>
							<input id="conPass" type="password" class="form-control is-invalid" name="confirmPassword">
							<div class="invalid-feedback">
								<%=error%>
							</div>
						<%}else{%>
							<input id="conPass" type="password" class="form-control" name="confirmPassword">
							<small class="form-text text-muted">Must match the password entered above</small>
						<%}%>
					</div>
					<button type="submit" class="btn btn-dark">Reset Password</button>
				</form>
		</div>
	</div>
</body>
</html>